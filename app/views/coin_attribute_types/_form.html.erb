<%= form_for(@coin_attribute_type) do |f| %>
  <% if @coin_attribute_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coin_attribute_type.errors.count, "error") %> prohibited this coin_attribute_type from being saved:</h2>

      <ul>
      <% @coin_attribute_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_area :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
